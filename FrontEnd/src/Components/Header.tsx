import UserAvatar from "../Features/Auth/UserAvatar";

const Header = () => (
  <header className="py-4 bg-secondary w-full flex items-center justify-end px-10">
    <UserAvatar />
  </header>
);

export default Header;
